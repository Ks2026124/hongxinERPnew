# DESIGN.md - 鸿信ERP 设计规范

## 项目与用户画像

- **产品**：鸿信ERP - 公司内部员工客户管理系统
- **用户**：公司管理层（管理员）和普通员工
- **场景**：日常办公，客户资料录入、团队管理、员工管理
- **设备**：电脑端为主，手机端适配

## 品牌与视觉方向

- **气质**：专业、沉稳、高效、可信赖
- **意象**：现代写字楼中的开放式办公空间 - 干净利落、秩序井然、信息清晰
- **调性**：商务极简，不花哨不冰冷，兼顾专业感与日常使用的舒适度

## Design Tokens

### 色彩

- **主色**：陶土橙（vintage-grey 主题），传递沉稳与可信赖
- **背景**：暖沙色/灰米色，类似实体纸张的舒适阅读体验
- **文字**：深灰色系，避免纯黑的生硬感
- **强调色**：仅用于操作按钮和关键状态提示
- **禁忌**：不使用蓝紫色渐变、不使用高饱和度色彩、不使用花哨的装饰色

### 字体

- **字体族**：PingFang SC / Hiragino Sans GB / Microsoft YaHei（系统默认中文商务字体）
- **标题**：加粗，字间距适中
- **正文**：常规字重，行高 1.6-1.8，保证长时间阅读的舒适度

### 圆角

- **组件圆角**：sm（克制、专业）
- **卡片圆角**：md
- **按钮圆角**：sm

### 阴影

- **阴影风格**：tool（标准实用主义），层级清晰但不夸张

## 布局与响应式

### 管理后台布局
- 左侧固定侧边栏导航（桌面端 240px，移动端收起为抽屉）
- 顶部栏：系统名称 + 用户信息
- 内容区域：最大宽度不限，自适应

### 认证页面布局（登录/注册）
- 居中卡片式布局
- 简洁表单，无侧边栏

### 响应式断点
- 移动端：< 768px（侧边栏收起，全宽内容）
- 桌面端：>= 768px（侧边栏展开）

## 组件规范

### 卡片
- 使用 `bg-card` 背景 + `border-border` 边框
- 内边距统一 `p-6`
- 标题使用 `text-lg font-semibold`

### 表格
- 使用 shadcn/ui Table 组件
- 表头使用 `bg-muted/50` 背景
- 行间距适中，保证数据可读性

### 表单
- 标签在输入框上方
- 输入框高度统一
- 错误提示使用 `text-destructive`

## 交互与状态

- **过渡动画**：统一使用 `transition-colors duration-200`
- **悬停状态**：背景色微调，不使用大幅位移
- **加载状态**：使用 Skeleton 组件占位

## 设计禁忌

- 不使用蓝紫色 AI 风格渐变
- 不使用圆角过大的组件
- 不使用花哨的动画效果
- 不使用纯黑纯白（使用主题色变量）
- 不使用装饰性图标（图标必须有功能含义）
